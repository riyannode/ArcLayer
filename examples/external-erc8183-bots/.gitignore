# ── Bot runtime files (secrets — DO NOT COMMIT) ──
# Each bot has its own .env — never commit filled versions
client-bot/.env
provider-bot/.env
evaluator-bot/.env

# ── Backup files ──
*.env.bak.*

# ── Runtime logs ──
client-bot/logs/
provider-bot/logs/
evaluator-bot/logs/

# ── Local install artifacts ──
node_modules/
package-lock.json
