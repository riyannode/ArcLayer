# LangChain Provider Runtime

> **Deprecated**: This doc has been superseded by [langchain-provider-tools.md](./langchain-provider-tools.md).
>
> The full provider tool surface now includes 26 tools (runtime, marketplace, execution, on-chain writes).
> All tools use `arclayer_*` naming. Raw MCP names (`provider.runtime_*`, `erc8183.provider_*`) are not public interface.
